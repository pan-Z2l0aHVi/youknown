import { default as Content } from './components/editor-content'
import { default as Toolbar } from './components/editor-toolbar'
import { default as useEditor } from './hooks'

const Editor = {
	Content,
	Toolbar,
	useEditor
}

export default Editor
