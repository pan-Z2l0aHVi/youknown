# Overlay

## 代码演示

import { Button, Card, Overlay } from '@youknown/react-ui/src'
import { useState } from 'react'

---

### 基本用法

export function BasicUsage() {
  const [open, setOpen] = useState(false)
  const show = () => setOpen(true)
  const hide = () => setOpen(false)
  return (
    <>
      <Button onClick={show}>Show overlay</Button>
      <Overlay open={open} onCancel={hide}>
        <Card className="w-320px! h-200px! mt-15vh">Content</Card>
      </Overlay>
    </>
  )
}

<BasicUsage />

---

```tsx
import { Button, Card, Overlay } from '@youknown/react-ui'
import { useState } from 'react'

export default () => {
  const [open, setOpen] = useState(false)
  const show = () => setOpen(true)
  const hide = () => setOpen(false)
  return (
    <>
      <Button onClick={show}>Show overlay</Button>
      <Overlay open={open} onCancel={hide}>
        <Card className="w-320px! h-200px! mt-15vh">Content</Card>
      </Overlay>
    </>
  )
}
```

## API

### Overlay Props

| 属性            | 说明             | 类型                  | 默认值          |
| --------------- | ---------------- | --------------------- | --------------- |
| open            | 是否打开         | `boolean`             | `false`         |
| overlayClosable | 启用点击蒙层关闭 | `boolean`             | `true`          |
| escClosable     | 启用esc键关闭    | `boolean`             | `true`          |
| timeout         | 动画过渡时间     | `number`              | `225`           |
| unmountOnExit   | 关闭后销毁组件   | `boolean`             | `false`         |
| appendTo        | 插入到节点       | `HTMLElement \| null` | `document.body` |
| onCancel        | 关闭事件回调函数 | `() => void`          | -               |
| afterClose      | 关闭之后回调函数 | `() => void`          | -               |
