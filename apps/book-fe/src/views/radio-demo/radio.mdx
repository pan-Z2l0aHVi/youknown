# Radio

## 代码演示

import { useEffect, useState } from 'react'
import { Radio, Divider, Space, Switch, Tabs, Button } from '@youknown/react-ui/src'

---

### 基本用法

export function BasicUsage() {
  return (
    <Radio defaultValue={true}>
      Basic
    </Radio>
  )
}

<BasicUsage />

---

```tsx
import { Radio } from '@youknown/react-ui'
export default () => {
  return (
    <Radio defaultValue={true}>
      Basic
    </Radio>
  )
}
```

### 受控组件

export function Controlled() {
  const [val, setVal] = useState(false)
  return (
    <>
      <Radio value={val} onChange={setVal}>
        Controlled
      </Radio>
      <Divider />
      <Space>
        <Button onClick={() => setVal(true)}>Check</Button>
        <Button onClick={() => setVal(false)}>Uncheck</Button>
      </Space>
    </>
  )
}

<Controlled />

---

```tsx
import { Radio, Space, Divider, Button } from '@youknown/react-ui'
import { useState } from 'react'

export default () => {
  const [val, setVal] = useState(false)
  return (
    <>
      <Radio value={val} onChange={setVal}>
        Controlled
      </Radio>
      <Divider />
      <Space>
        <Button onClick={() => setVal(true)}>Check</Button>
        <Button onClick={() => setVal(false)}>Uncheck</Button>
      </Space>
    </>
  )
}
```

### 复选框大小

export function RadioSize() {
  return (
    <Space>
      <Radio size="small" defaultValue={true}>
        Small
      </Radio>
      <Radio size="medium" defaultValue={true}>
        Medium
      </Radio>
      <Radio size="large" defaultValue={true}>
        Large
      </Radio>
    </Space>
  )
}

<RadioSize />

---

```tsx
import { Radio, Space } from '@youknown/react-ui'

export default () => {
  return (
    <Space>
      <Radio size="small" defaultValue={true}>
        Small
      </Radio>
      <Radio size="medium" defaultValue={true}>
        Medium
      </Radio>
      <Radio size="large" defaultValue={true}>
        Large
      </Radio>
    </Space>
  )
}
```

### 复选框组

export function RadioGroup() {
  const [groupVal, setGroupVal] = useState(2)
  const [dir, setDir] = useState('horizontal')
  return (
    <>
      <Tabs
        type="segment"
        tabList={[
          { key: 'horizontal', name: 'Horizontal' },
          { key: 'vertical', name: 'Vertical' }
        ]}
        value={dir}
        onChange={setDir}
      />
      <Divider />
      <Radio.Group
        direction={dir}
        options={[
          { label: 0, child: 'Cherry' },
          { label: 1, child: 'Apple' },
          { label: 2, child: 'Pear' },
          { label: 3, child: 'Orange', disabled: true }
        ]}
        value={groupVal}
        onChange={setGroupVal}
      />
    </>
  )
}

<RadioGroup />

---

```tsx
import { useState } from 'react'
import { Radio } from '@youknown/react-ui'

export default () => {
  const [groupVal, setGroupVal] = useState(2)
  const [dir, setDir] = useState<'horizontal' | 'vertical'>('horizontal')
  return (
    <Radio.Group
      direction={dir}
      options={[
        { label: 0, child: 'Cherry' },
        { label: 1, child: 'Apple' },
        { label: 2, child: 'Pear' },
        { label: 3, child: 'Orange', disabled: true }
      ]}
      value={groupVal}
      onChange={setGroupVal}
    />
  )
}
```

### 禁用状态

export function RadioDisabled() {
  const [disabled, setDisabled] = useState(true)
  return (
    <>
      <Switch value={disabled} onChange={setDisabled} />
      <Divider />
      <Space direction="vertical">
        <Radio disabled={disabled} defaultValue={true}>Disabled</Radio>
        <Radio.Group
          disabled={disabled}
          defaultValue={0}
          options={[
            { label: 0, child: 'A' },
            { label: 1, child: 'B' },
            { label: 2, child: 'C' }
          ]}
        />
      </Space>
    </>
  )
}

<RadioDisabled />

---

```tsx
export default () => {
  const [disabled, setDisabled] = useState(false)
  return (
    <>
      <Switch value={disabled} onChange={setDisabled} />
      <Divider />
      <Space direction="vertical">
        <Radio disabled={disabled} defaultValue={true}>Disabled</Radio>
        <Radio.Group
          disabled={disabled}
          defaultValue={0}
          options={[
            { label: 0, child: 'A' },
            { label: 1, child: 'B' },
            { label: 2, child: 'C' }
          ]}
        />
      </Space>
    </>
  )
}
```

### JSX

export function RadioJSX() {
  const [groupVal, setGroupVal] = useState(3)
  return (
    <>
      <Radio.Group
        direction="vertical"
        options={[
          { label: 0, child: 'Options A' },
          { label: 1, child: 'Options B' },
          { label: 2, child: 'Options C' }
        ]}
        value={groupVal}
        onChange={setGroupVal}
      >
        <Radio label={3}>JSX A</Radio>
        <Radio label={4}>JSX B</Radio>
        <Radio label={5}>JSX C</Radio>
      </Radio.Group>
    </>
  )
}

<RadioJSX />

---

```tsx
export default () => {
  const [groupVal, setGroupVal] = useState(3)
  return (
    <>
      <Radio.Group
        direction="vertical"
        options={[
          { label: 0, child: 'Options A' },
          { label: 1, child: 'Options B' },
          { label: 2, child: 'Options C' }
        ]}
        value={groupVal}
        onChange={setGroupVal}
      >
        <Radio label={3}>JSX A</Radio>
        <Radio label={4}>JSX B</Radio>
        <Radio label={5}>JSX C</Radio>
      </Radio.Group>
    </>
  )
}
```

## API

### Radio Props

| 属性       | 说明               | 类型              | 默认值   |
| ---------- | ------------------ | ----------------- | -------- |
| disabled   | 禁用状态           | `boolean`         | `false`  |
| size       | 大小               | `'small'`         | `'small' \| 'medium' \| 'large'`  |
| label    | 标识id           | `string \| number`         | -  |
| defaultValue    | 默认值           | `boolean`         | `false`  |
| value    |  是否选中          | `boolean`         | -  |
| onChange    |  选中事件回调          | `(value: boolean) => void`         | -  |

### Radio.Group Props

| 属性       | 说明               | 类型              | 默认值   |
| ---------- | ------------------ | ----------------- | -------- |
| disabled   | 禁用状态           | `boolean`         | `false`  |
| size       | 大小               | `'small'`         | `'small' \| 'medium' \| 'large'`  |
| defaultValue    | 默认值           | `string \| number`         | `[]`  |
| value    | 选中值           | `string \| number`         | -  |
| onChange    | 选中事件回调           | `(value: string \| number) => void`         | -  |
| direction    | 方向           | `'vertical' \| 'horizontal'`         | `'horizontal'`  |
| options    | 可选项列表           | `Options`         | `[]`  |

### Options

| 参数       | 说明               | 类型              | 默认值   |
| ---------- | ------------------ | ----------------- | -------- |
| disabled   | 禁用状态           | `boolean`         | `false`  |
| label    | 标识id           | `string \| number`         | -  |
| child    | 子元素           | `React.ReactNode`         | -  |
